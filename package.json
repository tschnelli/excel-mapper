{
  "name": "excel-mapper",
  "version": "1.0.0",
  "type": "module",
  "description": "Excel Mapper with authentication and subscription",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "stripe": "^12.0.0",
    "@clerk/nextjs": "^4.29.0",
    "micro": "^10.0.1"
  }
}
